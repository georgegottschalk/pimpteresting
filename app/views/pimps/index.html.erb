<h1>Listing pimps</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @pimps.each do |pimp| %>
      <tr>
        <td><%= pimp.description %></td>
        <td><%= link_to 'Show', pimp %></td>
        <td><%= link_to 'Edit', edit_pimp_path(pimp) %></td>
        <td><%= link_to 'Destroy', pimp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pimp', new_pimp_path %>
